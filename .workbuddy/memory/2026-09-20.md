# 2026-09-20 工作日志

## 生命图谱（product-lifecycle-graph）新增「运营周期」视图
- 需求：呈现部分产品多次 上架→下架→再上架 循环，完整展示每一次上架/下架区间与先后顺序，清晰区分在售/停售阶段，妥善处理状态切换。
- 实现：
  - `index.tsx` 新增 `buildServiceCycles`（由 `id % 3` 派生再上架轮次 0~2，生成交替 上架/下架 时间序列；按当前阶段处理 未上架/在售中/停售中/单次上架/多次循环；起止日期由标识码伪随机稳定派生，进行中区间按更新时间推算已持续天数）；新增 `ServiceCyclePanel`（甘特时间轴 + 明细表 + 图例 + 状态标签 + 空态）；顶部「流转图谱 / 运营周期」视图切换；流转图谱 上架/下架 节点新增 ×N 角标与运营周期联动；上架/下架抽屉补充说明。
  - `graph-style.css` 补充视图切换、甘特轨道/区间/标签、明细表样式。
  - 同步更新本页 `spec.md`（§1/§2/§5/§6/§7）与 `product-lifecycle/spec.md` §12、`change.md`。
- 验证：tsc 通过（仅剩 3 处预存无关错误：data-resource、codeReviewPlugin）；单独脚本校验周期推导在 安全审查/登记(空态)、上架(在售中)、交付(2上架1下架)、下架(3上架3下架停售中) 各场景均正确、无 NaN。
