## 2026-09-20
1、查看弹窗与「数据资源目录」列表查看弹窗在结构与展示逻辑上保持完全一致：弹窗标题「数据资源详情」，页签栏右上角常驻【查看存证】主按钮（唤起「区块链存证」子弹窗，z-index 1100），弹窗底部【关闭】按钮；移除原「目录详情」纯字段网格弹窗。
2、查看弹窗 Tab 按目录来源动态渲染：仅含「授权运营平台」→【基本信息】【审核信息】；仅含「政务信息资源」→ 仅【政务信息资源】；同时含两者 →【基本信息】【政务信息资源】【审核信息】。各 Tab 内容字段与数据资源目录对应弹窗保持一致（基本信息 / 政务信息资源 / 审核信息）。
3、为保持字段集一致，`CatalogRecord` 类型新增可选字段 `reviewStatus`、`mountStatus`（当前 seed 数据未填充，展示为「-」）。查看弹窗专属样式（detail-tabs、resource-detail-modal、detail-readonly-table、detail-info-table、detail-audit-table、proof-modal 等）按 data-resource-catalog/style.css 对齐复制到本页 style.css，未引入新依赖、不影响其他页面。

## 2026-09-20（补充）
4、查看弹窗【查看存证】按钮仅在目录来源包含「授权运营平台」时显示；当来源仅含「政务信息资源」时不显示该按钮，与其余来源组合（仅授权运营平台 / 同时含两者）的展示逻辑一致。采用条件渲染（保留按钮原始代码，满足条件才挂载），未删除代码、未引入新依赖。

## 2026-09-03
1、新增原型修改记录（change.md），并在顶部栏接入「原型修改记录」入口。
2、页面说明文档补充为与其他后台页面一致的九段式结构。

## 2026-09-02
1、新增统一目录查询页面，作为后台一级菜单（位于「工作台」之后、「备案管理」分组之前），分配给数据管理部门角色，仅提供查询与查看权限。
2、页面自上而下为统计卡片、筛选查询区、列表表格与分页；顶部两张卡片分别展示资源持有方数量与目录总数。
3、筛选条件支持资源名称、资源持有方、目录来源、更新时间（起止日期）；起始日期晚于结束日期时前端提示「起始日期不能晚于结束日期」，不执行查询。
4、列表展示序号、资源名称、资源持有方、目录来源、更新时间，操作列仅保留「查看」，弹窗只读展示该目录条目的完整元数据。
5、目录来源支持授权运营平台、资源登记平台、政务信息资源三个取值，单条记录可配置多个来源，筛选时按包含关系匹配。
6、新增页面说明文档 spec.md。
