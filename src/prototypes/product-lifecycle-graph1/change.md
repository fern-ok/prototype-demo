## 2026-09-20
新建「生命图谱1」独立整页原型（与「生命图谱」并列的线性时间线展现形式）。
1、新建 `index.tsx`：通过 `?code=` 定位记录，构建**线性时间线**——严格按时间先后顺序线性排列流程节点，仅展示已实际执行过的步骤，未执行步骤不显示，且无任何支线 / 分支 / 回路（如 上架 → 变更 → 下架 → 上架 单一序列）。节点视觉完全复用「生命图谱」的圆角矩形 SVG 节点（编号、阶段名、分类标签、箭头连线），但节点严格水平排成一条直线；多次上架 / 下架 / 变更以右上角「×N」徽标区分。节点点击从右侧抽屉展示阶段说明、发起 / 审批信息与操作时间；顶栏展示标识码 / 名称 / 当前阶段与分类图例，并提供「返回产品生命周期」入口。
2、新建 `graph1-style.css`：定义线性图谱专属提示与画布微调，节点 / 箭头 / 抽屉样式复用「生命图谱」`graph-style.css`。
3、复用 `lifecycle-shared.tsx` 的 `getRecordByCode` / `getStageStatusClass`；样式复用 `product-lifecycle/style.css`、`product-lifecycle-lineage/lineage-style.css`、`product-lifecycle-graph/graph-style.css`，新增样式仅本页生效，未引入新依赖、未改动其他页面既有逻辑。
4、新建 `spec.md` 与本文档，说明线性时间线形态、入口规则与「生命图谱」一致的视觉样式。
5、固化第一条数据演示序列：列表页按「更新时间」倒序后的首行记录（同时兼容截图中的标识码 `DP430300202602019`）进入「生命图谱1」时，固定渲染 20 步完整线性序列——开发 → 编目 → 安全审查 → 登记 → 变更(登记后) → 上架① → 变更(上架后) → 交易① → 下架① → 上架② → 变更③ → 交易② → 下架② → 上架③ → 变更④ → 交易③ → 下架③ → 上架④ → 交易④ → 撤销；所有步骤状态为「已完成」，最后一步「撤销」标记为当前节点，时间顺序严格递增，无任何分支 / 回路。
6、图谱改为**每行最多 8 个节点**自动换行：超出 8 个的后续节点折至下一行，采用「蛇形／回形」布局——第 1 行从左到右，第 2 行从右到左，第 3 行再从左到右，依此类推；换行处通过垂直连线衔接，箭头位于两行之间的转折点（行间隙中点）并指向下一行，保持单一时间序列、无分支 / 回路。SVG viewBox 高度按行数动态计算。
7、优化连线和箭头位置：同一行内连线从源节点边缘连到目标节点边缘（奇数行自动反向）；换行处改为「下 → 横 → 下」的阶梯连线，奇数行整体向左错开 28px，使转折处形成明显水平段；箭头置于水平转折段，指向下一行阅读方向（右→左指向左、左→右指向右），与参考图中红色箭头形式保持一致。
8、修复连线消失缺陷：修正 `renderEdge` 中节点中心坐标计算错误（原实现误将 `nodeX` 中心坐标又加了半个节点宽度当作中心使用，导致同一段连线的起点/终点严重错位、行内连线被节点遮挡或超出画布而不可见），统一按 `nodeX` 返回的中心坐标计算边缘位置与转折垂线，连线恢复正常显示。
