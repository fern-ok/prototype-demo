## 2026-09-21
1、资源目录关联弹窗：已选中资源芯片上移除「当前选中」文字提示；改为在芯片资源名称旁提供删除图标（×），点击即可从选中列表中移除该资源，不影响点击查看详情，弹窗其余布局与交互逻辑保持不变。
2、新增弹窗关联资源标签样式调整：已关联政务信息资源目录标签改为蓝色填充背景（#1464f4）、白色文字、圆角 14px 胶囊样式；删除图标进一步与关联弹窗芯片的删除图标完全对齐——容器由 `<button>` 改为 `<span>`、尺寸 16×16px、图标 `X size={12} strokeWidth={2.5}`、透明背景、`currentColor` 白色、hover 半透明白色背景，整体视觉与交互与关联弹窗已选中芯片保持一致。
3、操作列「变更记录」入口扩展至「已撤销」状态：状态为已撤销且已发生过变更的记录，「查看」之后展示「变更记录」按钮，点击打开变更记录弹窗查看该记录的变更历史（第 9 条「已撤销-林木育苗数据」补充 2 条林业场景变更记录与全量字段对比快照，演示该入口）。
4、目录默认关联政务目录资源：Resource 类型新增 `govCatalog` 字段；全部已有目录（第 1–10 条）均绑定一条对应政务目录资源，新建目录在新增弹窗默认预置一条已关联政务目录（取 seedGovCatalogs[0]），编辑／变更弹窗回显已绑定资源；列表「关联」弹窗打开时以 `initialAssociated` 预置已绑定资源，详情弹窗「政务信息资源」页签内容改为取该目录已绑定的政务目录资源（资源名称、数据所属领域、资源格式、更新频率、覆盖时间范围、地域分类、资源摘要），未关联时展示空态提示。
5、修复资源目录关联弹窗取消关联后名称残留缺陷：弹窗顶部「已关联政务信息资源目录」芯片列表不再直接渲染不可变的 `initialAssociated`，改为基于当前 `selected` 选中状态实时渲染；用户点击芯片删除图标或在表格中取消勾选后，`selected` 立即移除对应 ID，芯片列表同步移除该资源名称，确保界面展示与最新关联数据保持一致。

## 2026-09-20
1、资源目录关联弹窗：隐藏「数据所属领域」列与「操作」列（含【查看】入口），原始字段与入口保留，仅以 display:none 不展示，去掉两列后表格可见列为 复选框、序号、资源名称、资源摘要。
2、数据资源详情弹窗：在「审核信息」页签前新增「政务信息资源」页签，内部含「基本信息」（资源名称、资源格式、更新频率、覆盖时间范围、地域分类、资源摘要）与「信息项」（序号、信息项英文名、信息项名称、数据类型、数据长度）两段，格式参考「基本信息」页签。页签顺序调整为：基本信息 → 政务信息资源 → 审核信息。
3、数据资源详情弹窗「基本信息」页签下的「信息项」表格新增「序号」列（置于最左侧），字段顺序为：序号、信息项英文名、信息项名称、数据类型、数据长度、信息项说明；序号从 1 开始连续递增，与信息项实际展示顺序保持一致。

## 2026-09-16
1、联系人字段的校验条件完善：请输入2-50位由汉字、间隔号组成的字符;间隔号不能在首尾、不可连续。
间隔号规则：仅可输入汉字与中间标准间隔号`·`；间隔号不可在开头、末尾，不可连续输入，间隔号两侧不能带空格

## 2026-09-08
数据资源目录变更撤销业务涉及实施机构、数源机构两个角色
## 变更
1、已授权状态下可以变更基本信息中除了资源名称、资源格式以外的信息。
2、如果变更了信息项（除信息项说明），需要判断是否挂载了数据源，如果挂载了，需要将挂载状态置为未挂载，重新通知开发平台进行数据源挂载。
3、变更弹窗没有“存为草稿”按钮。
4、变更弹窗中增加变更说明的输入框（必填，512个字符）。
5、提交过变更的记录，操作栏展示变更记录入口。变更记录中，变更前文字和背景不用标红。对于变更后的内容，修改、新增：文字标红，背景不标红。删除：背景色标红（信息项删除不用标红）。
6、若什么都没变，点击提交按钮，提示：“当前无变更内容，请修改后再提交”。只有全部字段与原值完全一致才算无变更
7、存证：增加 "资源变更登记审核通过后自动上链" 机制，并将 "数据持有方" 更名为 "资源持有方"。
8、审核状态：待登记、首次登记待审核、变更登记待审核、撤销登记待审核、首次登记未通过、变更登记未通过、撤销登记未通过、已通过、已撤销。
同步修改以下页面的审核状态字典：运营机构、其他经营主体的产品登记页面；数据管理部门、行业主管部门的数据资源编目监管、产品登记监管页面。
9、新增4条消息通知模版（序号8-11）：变更登记申请审核通过、变更登记申请审核不通过、撤销登记申请审核通过、撤销登记申请审核不通过。
## 撤销
1、存在于数据资源授权单的资源不允许撤销，提示：当前资源已提交授权，不支持撤销。
2、撤销弹窗展示资源名称（禁用）、撤销说明（必填，512个字符）。
## 查看
1、详情弹窗新增「审核信息」页签：默认激活「基本信息」页签，可切换至「审核信息」查看；内容区为审核流程表格（序号、流程节点、节点状态、单位名称、法人经办人姓名、操作时间、审核结果、审核意见）

## 优化
1、减小「原型修改记录」弹窗内容行间隔，使记录条目展示更紧凑。

## 2026-09-03 
1、地域分类去掉“全国”选项，因为全域不支持6个0了，以前全国是6个0，现在没有全国的概念了
2、实施机构、数源机构，新增资源编目信息项旁边增加一句红色提示："信息项英文名须与挂载的数据源字段名完全一致（区分大小写）"
