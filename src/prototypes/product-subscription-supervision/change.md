## 2026-09-08
新增「产品订阅监管」页面，作为「授权监管」菜单项下的二级子菜单。
1、页面标题：产品订阅监管；模块归属：授权监管。
2、筛选条件：所属地域、授权运营类型、领域名称、产品名称、产品类型、产品提供方、订阅次数；操作按钮为【查询】【重置】。
3、列表字段：序号、所属地域、授权运营类型、领域名称、产品名称、产品类型、产品提供方、订阅次数、操作。
4、操作列按钮：查看、查看订阅明细。
   - 查看：打开产品详情弹窗（只读，展示产品名称、产品类型、所属地域、授权运营类型、领域名称、产品提供方、订阅次数、产品简介）。
   - 查看订阅明细：打开订阅明细弹窗，展示订单编号、数据需求方、订阅时间、订单状态（已下单、交付中、已完成、已终止、已冻结）、更新时间，并自带分页。
5、分页：共 XXX 条记录 + 上一页/页码/下一页 + 每页条数 + 跳页，默认 10 条/页、默认第一页。
6、页面初始化不带筛选条件加载全量数据；重置后恢复全量数据；无数据时表格展示「暂无数据」。
7、后台侧边栏新增「授权监管」菜单组，并挂载「产品订阅监管」二级菜单。

## 2026-09-09
「订阅明细」弹窗新增筛选区，沿用列表契约风格（内联 label + input，3 列网格，无外框）。
1、筛选条件：订单编号（文本模糊搜索）、数据需求方（文本模糊搜索）、订单状态（下拉，枚举与列表一致：已下单、交付中、已完成、已终止、已冻结）、更新时间（日期范围，开始日期 - 结束日期）。
2、操作按钮：查询（蓝主按钮）、重置（白次按钮），重置清空全部 4 项筛选条件。
3、筛选与明细分页、每页条数正确联动：切换条件/切换每页条数/重置均会回到第 1 页。
4、打开订阅明细弹窗时自动重置筛选条件。

## 2026-09-09（追加）
「订阅明细」弹窗顶部新增产品概要区，替换原产品信息条。
1、字段：所属地域、授权运营类型、领域名称、产品名称、产品类型、产品提供方（共 6 项，3 列 × 2 行网格；宽屏 3 列，`≤1024px` 降级 2 列，`≤640px` 降级 1 列）。
2、数据来源：新增 `OrderDetailProduct` 类型 + `fetchOrderDetailProduct()`（保留 Promise 形态便于后续切换为真实接口），字段名与后端一致。
3、视觉：浅灰背景 `#fafbfc` + 细边框 `#edf0f5` 与下方明细表格区隔；标签 13px 灰字右对齐 + 冒号，值 13px 黑字左对齐。
4、加载态：6 个骨架占位带 shimmer 动画；接口失败时降级为「-」占位，不渲染错误。
5、边界：空值显示「-」；长值单行省略 + `title` 悬浮展示完整内容。

## 优化
1、补齐「订阅明细」订单状态色：`已下单`（蓝）、`交付中`（橙）、`已完成`（绿）、`已终止`（红）、`已冻结`（灰）。此前 `交付中` / `已冻结` 对应的 `.status-processing` / `.status-frozen` 在公共列表契约 `backend-list.css` 中未定义，导致无配色；现按 `subscription-order-supervision` 页已有的色值在本页 `style.css` 中补齐，与同类页面保持一致。
2、列表新增「产品销售总额（分）」列，位于「下单次数」之后、操作列之前。
   - 单位固定为**分**，整数存储与展示，不做隐式单位换算；展示添加千分位分隔符（如 `12,845,600`）。
   - 表头标注单位：`产品销售总额（分）`；数字右对齐、内容不换行、列宽 150px 自适应，启用 `tabular-nums` 等宽数字。
   - 新增 `salesAmount: number` 字段与 `formatAmountInCents()` 格式化函数；空态 `colSpan` 由 9 调整为 10。
3、「订阅明细」表格新增「订单总额（分）」列，位于「订单状态」之后、「更新时间」之前。
   - 单位固定为**分**，整数存储与展示，不做隐式单位换算；复用 `formatAmountInCents()` 做千分位分隔。
   - 样式复用金额规范：`.data-table .col-order-amount` 与列表金额列同规则（右对齐、不换行、`tabular-nums` 等宽数字）。
   - `SubscriptionOrder` 新增 `orderAmount: number` 字段；明细表格由 5 列增至 6 列，空态 `colSpan` 现与实际列数一致（6）。
4、列表上方新增统计概览区（筛选区之上），横向四列：可交易数据产品（个）、累计生成订单（条）、已交付订单（条）、累计调用次数（次）。
   - 沿用列表卡片外壳：白底、`#dfe5ef` 细边框、4px 圆角、`var(--layout-shadow)` 阴影、`margin: 0 18px 10px`。
   - 数值千分位格式化（新增通用 `formatThousands()`，金额列 `formatAmountInCents()` 改为复用它，行为不变）；`tabular-nums` 等宽数字。
   - 桌面端一行四列 + 项间竖分隔线；`≤1024px` 降级两列；`≤620px` 降级单列。
   - 统计口径为**全量统计**，数据来自模块级常量 `OVERVIEW_STATS`，不随列表筛选条件联动。

## 2026-09-10
（承上）统计概览区随列表页一并交付，统计口径明确为全量、不联动筛选。
1、「订阅明细」表格在「更新时间」之后新增「操作」列，放置【调用明细】按钮。
   - 显示条件：`当前明细所属产品类型为「API产品」` 时才渲染按钮；数据集等非 API 产品的订单不显示该按钮（列保留、单元格为空）。
2、点击【调用明细】打开调用明细弹窗，内容与「订单交付监管」页的调用明细一致：
   - 顶部延时提示 + 【手动刷新】；汇总统计（API接口名称、调用总次数、调用成功次数、调用失败次数，千分位）；筛选（调用状态、调用时间）+【查询】【重置】；表格（序号、调用状态、响应时长(ms)、调用时间、操作）+ 分页；加载中 / 失败兜底 / 暂无调用记录三态；失败行可【查看失败原因】。
   - 新增 `CallLog` 类型、`callLogsByOrder` / `callStatsByOrder`（按订单号索引，仅为 API 产品订单生成）、`CALL_STATUS_OPTIONS` / `TIME_RANGE_OPTIONS` / `TIME_RANGE_CUTOFF` / `CALL_ERRORS` 常量。
   - 样式 `.call-tip` / `.link-btn` / `.call-stats-grid` / `.call-filter-section` / `.call-status-fail` / `.call-log-table` 沿用订单交付监管页同套取值。
3、明细表格由 6 列增至 7 列，空态 `colSpan` 同步调整为 7。

