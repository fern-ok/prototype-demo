# 2026-09-11 工作日志

- **数据资源目录：移除「扩展码」三处前端展示**：查看详情弹窗基本信息表、变更弹窗、变更记录详情。
  - 详情表格（`index.tsx`）：删除 `扩展码` 那对 `th/td` 后重排为「前 12 项两列成对 + 数据源挂载状态与资源摘要各占 `colSpan={3}` 独占整行」，避免 `table-layout: fixed`（th 18% / td 32%）下出现残缺行导致列宽错位。
  - 变更弹窗（`ResourceForm`）：`扩展码` 表单项改为 `{mode !== 'change' && <Field .../>}`，新增／编辑仍保留输入与提交。
  - 变更记录详情：从 `baseBasicFields` 移除该字段，并移除第一条快照中的 `扩展码` deleted 差异。
  - 按要求保留 `FormInitial.extCode?: string` 类型定义与 `extCode` state，数据请求、接口字段、后端逻辑均未改动。
  - 文档：`spec.md` 追加「变更弹窗专属差异③（不展示扩展码）」与详情页「前 12 项」描述；`change.md` 追加 2026-09-11 条目。
  - 验证：`npm run typecheck` 无新增错误；`npm run build` 通过（29 个 HTML）；浏览器核验四处置——详情弹窗、变更弹窗、变更记录详情均无「扩展码」且行列布局对齐，新增弹窗仍包含「扩展码」。

- **构建与浏览器验证踩坑（可复用）**：
  - ① 构建脚本 `fs.rmSync(dist)` 走系统回收站，Windows 上偶发 `trash operation aborted`；解决办法是先把 `dist` 重命名为 `dist_bak_*`（脚本仅在 `dist` 存在时才删除），构建后再清理备份目录。
  - ② `pkill -f agent-browser` 会匹配到自身 shell 的命令行导致进程自杀（命令无输出、exit 1）；应写成 `pkill -f "agent-brow[s]er"`。
  - ③ esbuild 偶发 `write EPIPE` 服务中断，直接重跑一次 `npm run build` 即可恢复（本次全量构建约 14 分钟，需放后台等待）。
