## 2026-09-11
1、筛选「所属地域」升级为两级级联下拉（复用公共组件 `src/common/RegionCascader.tsx`）：第一级省本级 + 14 个市州（省本级为末级直接选中；市州行悬停展开第二级面板），第二级为对应区县，最低可选到区县级；点击单选钮或末级行完成选择，选中市州或区县均与列表「所属地域」精确匹配；点击组件外部关闭，【重置】清空。
2、演示数据调整：id=3「长沙市城市交通流量监测API」的所属地域由「长沙市」调整为「天心区」，用于演示区县级筛选命中效果。

## 2026-09-10
1、列表「订单总额（元）」展示固定保留两位小数：在千分位分隔基础上统一显示两位小数（如 `594,500.00`），存储仍为分、不做单位换算；同步将规格与历史记录中的表头单位标注由「（分）」修正为「（元）」，与当前代码一致。

## 2026-09-09
新增「订阅订单监管」页面，作为「授权监管」菜单项下的二级子菜单。
1、页面标题：订阅订单监管；模块归属：授权监管。
2、筛选条件：订单编号、所属地域、授权运营类型、领域名称、产品名称、产品类型、数据需求方、订单状态、更新时间；操作按钮为【查询】【重置】。
3、列表字段：序号、订单编号、所属地域、授权运营类型、领域名称、产品名称、产品类型、数据需求方、订单状态、更新时间、操作。
4、操作列按钮：查看、查看订单明细。
   - 查看：打开产品详情弹窗（只读，展示产品名称、产品类型、所属地域、授权运营类型、领域名称、产品提供方、订阅次数、产品简介）。
   - 查看订单明细：打开订单明细弹窗，展示该产品下的订单编号、数据需求方、订阅时间、订单状态、更新时间，并自带分页。
5、分页：共 XXX 条记录 + 上一页/页码/下一页 + 每页条数 + 跳页，默认 10 条/页、默认第一页。
6、页面初始化不带筛选条件加载全量数据；重置后恢复全量数据；无数据时表格展示「暂无数据」。
7、后台侧边栏「授权监管」菜单组新增「订阅订单监管」二级菜单，与「产品订阅监管」并列。

## 2026-09-09 迭代
1、订单状态枚举统一调整为：已下单、交付中、已完成、已终止、已冻结。
   - 同步修改范围：筛选区「订单状态」下拉选项、列表状态标签、订单明细弹窗状态标签、演示数据生成逻辑。
   - 状态标签配色：已下单（蓝色）、交付中（橙色）、已完成（绿色）、已终止（红色）、已冻结（灰色）。
2、列表默认按「更新时间」倒序排列，最新在前；查询、重置、翻页后排序保持不变。

## 2026-09-09 迭代2
1、操作列按钮调整为按产品类型动态展示：
   - 产品类型为 API产品：显示【查看】【调用明细】两个按钮。
   - 其他类型（数据集）：仅显示【查看】按钮（迭代3 已为数据集补充【下载明细】）。
   - 原【查看订单明细】按钮移除，由【调用明细】替代。
2、新增「调用明细」弹窗（仅 API 产品订单），自上而下分为：
   - 提示与统计汇总区：延时提示文案 + 蓝色「手动刷新」文字按钮（独立刷新汇总统计，刷新期间展示「统计中…」）；统计指标为 API接口名称、调用总次数、调用成功次数、调用失败次数（两行四列、标签灰底）。
   - 筛选查询区：调用状态（请选择/成功/失败）、调用时间（近三个月/近半年/近一年，默认近三个月），按钮【查询】【重置】。
   - 调用日志表格：序号、调用状态（成功黑色文字、失败红色高亮）、响应时长(ms)、调用时间（yyyy-MM-dd HH:mm）、操作（失败显示【查看失败原因】蓝色文字链接，成功操作列为空）。
   - 分页区：共 X 条记录 + 上一页/页码/下一页 + 每页条数（默认 10 条/页）+ 跳页。
   - 状态兜底：加载中展示「加载中…」、错误展示重试提示、无数据展示「暂无调用记录」。
3、新增「失败原因」弹窗：展示订单编号、API接口名称、调用时间、响应时长(ms)、调用状态、错误码、错误信息，只读。
4、调用明细视觉样式与交互复用现有弹窗契约（modal-large/modal-medium、backend-list.css 表格/分页/筛选样式），弹窗内筛选区收缩为两列布局。

## 2026-09-09 迭代3
1、操作列按钮为数据集产品补充【下载明细】：
   - 产品类型为 数据集：显示【查看】【下载明细】两个按钮。
   - 产品类型为 API产品：显示【查看】【调用明细】两个按钮（保持迭代2 不变）。
   - 非上述类型：仅显示【查看】按钮。
2、新增「下载明细」弹窗（仅数据集产品订单），与调用明细弹窗同一套视觉与交互契约，整体从上至下分为：
   - 统计汇总区：表名、下载总次数、下载成功次数、下载失败次数（两行四列、标签灰底，无手动刷新控件），独立展示不受表格分页与筛选控制。
   - 筛选查询区：下载状态（请选择/成功/失败）、下载时间（近三个月/近半年/近一年，默认近三个月），按钮【查询】【重置】。
   - 下载日志表格：序号、下载状态（成功黑色文字、失败红色高亮）、下载耗时(ms)、下载时间（yyyy-MM-dd HH:mm）、操作（失败显示【查看失败原因】蓝色文字链接，成功操作列为空）。
   - 分页区：共 X 条记录 + 上一页/页码/下一页 + 每页条数（默认 10 条/页）+ 跳页。
   - 状态兜底：加载中展示「加载中…」、错误展示重试提示、无数据展示「暂无下载记录」。
3、新增「下载明细-失败原因」弹窗：展示订单编号、表名、下载时间、下载耗时(ms)、下载状态、错误码、错误信息，只读。
4、下载明细视觉样式与交互复用现有弹窗契约（modal-large/modal-medium、backend-list.css 表格/分页/筛选样式），弹窗内筛选区收缩为两列布局。

## 2026-09-09 迭代4
1、列表新增「订单总额（分）」字段，置于「订单状态」之后、「更新时间」之前。
   - 字段含义：订单交易总金额，单位分（整数），列表以千分位格式展示。
   - 演示数据按订单稳定生成（约 50 万 ~ 104 万区间，单位分），与订单一一对应。
