# 原型修改记录

## 2026-09-14（补充 4）

- 「数据资源行业分布」由水平条形图改为环形图（参考新版效果图）：`IndustryBar` 替换为 `IndustryDonut`（ECharts 环形饼图，9 个行业扇区，9 色多彩配色），面板布局改为左侧圆环（42% 宽）+ 右侧 3×3 色点图例（`.industry-legend-grid`），图例颜色与扇区一一对应；数据保持 industryBars 不变。

## 2026-09-14（补充 3）

- 大屏自适应重构：改为「固定设计稿 + 等比缩放」方案，修复不同分辨率下内容被截断（底部面板超出视口）或图表拉伸错乱的问题。
  - `index.tsx`：新增 `screen-viewport` 视口容器与 1920×1080 固定舞台，JS 监听 resize 计算 `scale = min(视口宽/1920, 视口高/1080)`，对舞台整体 `transform: scale()` 居中缩放。
  - `style.css`：舞台固定 1920×1080（flex 纵向布局，`content-wrap` 改为 `flex:1` 自动填充），移除原 1366px / 1280px 流式断点；视口留边区域填充同款深蓝渐变背景。
  - 效果：任意分辨率与宽高比下全部内容等比完整呈现，无溢出、无截断、无滚动条、无元素重叠。

## 2026-09-14（补充 2）

- 修复两处显示缺陷：
  1. 背景重影/重复渲染：移除整页背景层 `bg-map`（此前在全屏背景又渲染了一次含领域气泡的湖南地图，与中间面板「数据资源领域名称分布」重复显示两份），地图现仅在中间面板渲染一次；背景只保留 HUNAN 发光字样。
  2. 图例溢出：「再开发数据产品订阅行业占比」面板的环形图容器原占满 `panel-body` 高度（height:100%），图例被挤出面板、掉落到下方「订阅趋势分析」面板标题上。新增 `.ring-wrap` 纵向 flex 容器包裹环形图与图例，图表 `flex:1` 自适应收缩、图例固定于面板内部，不再溢出可视区。

## 2026-09-14

- 主区域布局重构（参考新版效果图）：取消原「中部三栏 + 底部三栏」两行结构，改为 `row-main` 三列布局——
  - 中列"数据资源领域名称分布"湖南地图纵向贯通，占满指标卡以下全部高度；
  - 右列自上而下堆叠三个面板：数据资源行业分布、基础数据产品授权次数 TOP10、再开发数据产品订阅次数 TOP10；
  - 左列保持两个面板：再开发数据产品订阅行业占比（上）、再开发数据产品订阅趋势分析（下）。
  - `index.tsx`：内容区重组为三个 `main-col`；`style.css`：`content-wrap` 改为 `auto 1fr` 两行，新增 `.row-main`（29% / 42% / 29%）与 `.main-col` 纵向 flex 及各面板高度权重，同步更新 1366px / 1280px 断点间距。

## 2026-09-11

- 修复监控大屏顶部统计指标区布局：
  - `src/prototypes/monitoring-dashboard/style.css`：将 `.row.row-stats` 改为单列布局，避免 `StatCards` 被外层 `repeat(4, 1fr)` 限制为仅占 25% 宽度。
  - `.statistic-list` 使用 `grid-template-columns: repeat(4, minmax(0, 1fr))`，确保 4 个统计卡片在水平方向占满整行且均匀分布。
  - `.stat-info` 改为双列等宽网格布局，子指标项禁止换行并防止溢出。
  - 新增/完善 1366px 与 1280px 响应式断点，缩放字体、间距与图标尺寸，保证不同分辨率下统计项铺满整行、对齐整齐、避免换行或溢出。

## 2026-09-08（补充 2）

- 监控大屏改为独立全屏页面：移除 `PortalLayout` 包裹，去掉门户公共顶部导航栏与页脚，整页仅保留大屏本体；同步移除页面上"页面说明""原型修改记录"入口，规格与记录改为直接维护 `spec.md` / `change.md` 文件。

## 2026-09-08

- 新建 `src/prototypes/monitoring-dashboard/` 监控大屏原型。
- 参考资料：`C:/Users/Lenovo/Desktop/监控大屏html.txt`、`C:/Users/Lenovo/Desktop/监控大屏.txt`、用户提供的监控大屏截图。
- 实现五大区域：顶部 4 个核心指标卡、中部环形图 + 湖南地图 + 行业条形图、底部折线图 + 两组 TOP10 条形图。
- 图表渲染统一使用项目已安装的 echarts（^6.0.0），未新增依赖。

## 2026-09-08（补充）

- 在后台管理系统侧边栏（公共布局 `src/common/Layout.tsx`）新增「监控大屏」一级菜单，置于「工作台」之后。
- 菜单链接指向 `/prototypes/monitoring-dashboard.html`，并设置 `target="_blank" rel="noopener noreferrer"`，点击后另开新页签全屏展示监控大屏。