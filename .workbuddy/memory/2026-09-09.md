# 2026-09-09 工作日志

- **「产品订阅监管」订阅明细新增筛选区**：在 `src/prototypes/product-subscription-supervision/index.tsx` 的「订阅明细」弹窗（产品信息条与表格之间）新增 4 个筛选条件：订单编号（文本模糊）、数据需求方（文本模糊）、订单状态（下拉，枚举与列表一致：已下单/交付中/已完成/已终止/已冻结）、更新时间（日期范围）。`handleViewDetail` 打开时自动重置筛选，【查询】/【重置】按钮与列表契约一致（蓝主/白次），筛选与明细分页、每页条数联动（切换条件/切换页大小/重置均回到第 1 页）。`style.css` 新增 `.detail-filter*` 段，按图片风格采用内联 label + input 的 3 列网格布局（无外框）。同步更新 `spec.md` 新增 §6.1 明细筛选说明，`change.md` 追加 2026-09-09 条目。`tsc --noEmit` 无新增错误，dev server 编译通过。
- **「订阅明细」弹窗顶部新增产品概要区**：替换原 `.detail-product-tip` 蓝条为 `.detail-summary` 浅灰卡片，6 字段（所属地域/授权运营类型/领域名称/产品名称/产品类型/产品提供方）按 3 列 × 2 行网格排列，宽屏 3 列、`≤1024px` 降级 2 列、`≤640px` 降级 1 列；标签 13px 灰字右对齐 + 冒号，值 13px 黑字左对齐。数据通过新增 `OrderDetailProduct` 接口类型 + `fetchOrderDetailProduct()`（Promise 形态）读取，字段名与后端一致；加载态显示 6 个 shimmer 骨架，接口失败时降级为「-」占位不渲染错误；空值显示「-」，长值 `text-overflow: ellipsis` + `title` 悬浮。`spec.md` / `change.md` 同步追加说明。`tsc --noEmit` 无新增错误，dev server 编译通过。
- **补齐订单状态色（项目通用约定）**：`backend-list.css` 仅定义 `status-approved`(绿) / `status-pending|reviewing|default`(蓝) / `status-rejected`(红)，`status-processing`(橙) 与 `status-frozen`(灰) 未覆盖，需在各自页面 `style.css` 补充。已按 `subscription-order-supervision` 页既有色值（`#b26a00/#fff6e8/#ffdfae` 与 `#6b7280/#f3f4f6/#e2e5ea`）在 `product-subscription-supervision/style.css` 补齐，保证同类页面配色一致。
- **「产品订阅监管」列表新增金额列**：新增 `salesAmount: number`（单位「分」，整数，不做单位换算）+ `formatAmountInCents()` 千分位格式化函数；表头「产品销售总额（分）」，位置在「下单次数」之后、操作列之前；样式 `.data-table .col-sales-amount` 宽 150px、`text-align: right`、`white-space: nowrap`、`font-variant-numeric: tabular-nums`（注意：需带 `.data-table` 前缀才能覆盖 `.data-table th { text-align: left }`）。空态 `colSpan` 9→10。`spec.md` 新增 §5.1 金额字段规范，`change.md` 追加优化条目。
- **「订阅明细」表格新增「订单总额（分）」**：`SubscriptionOrder` 新增 `orderAmount: number`（单位：分），位置在「订单状态」之后、「更新时间」之前；复用 `formatAmountInCents()` 与金额列样式（`.data-table .col-order-amount` 与 `.col-sales-amount` 合并为同一条规则）。明细表格 5 列 → 6 列，空态 `colSpan` 原本为 6（与实际 5 列不一致），加列后正好匹配。`spec.md` §6.0、`change.md` 同步。
- **页面命名口径**：用户口中的「产品订单监管」实指「产品订阅监管」（`product-subscription-supervision`）——项目里「订阅明细」只存在于该页；另一页 `subscription-order-supervision` 标题是「订单交付监管」，含「调用明细」「下载明细」，不要混淆。
