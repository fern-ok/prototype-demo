# 2026-08-28

## 数据资源目录 - 操作按钮按审核状态调整
- 文件：`src/prototypes/data-resource-catalog/index.tsx` + `style.css`，同步更新 `spec.md`。
- 实现 7 个操作按钮，可见性由 `ALL_ACTIONS`（带 `show` 谓词）控制：
  - 查看：全部状态；查看存证：变更未通过/撤销未通过/已通过；关联：除已撤销外；编辑/删除：待提交/提交未通过；变更/撤销：变更未通过/撤销未通过/已通过。
- 「查看」改为「数据资源详情」弹窗，只读展示完整信息 + 审核流转记录（时间线）；「关联/编辑/删除/变更/撤销」先用确认/信息弹窗占位（删除为危险红色按钮）。
- typecheck 通过（该文件无新增报错；其余报错为历史遗留，与本次无关）。

## 数据资源目录 - 新增弹窗改为左右两列布局
- 「资源摘要」字段加 `full` 属性跨整行（`Field` 组件支持 `full?: boolean`）；`form-grid` 由 `1fr 1fr` 改成 `repeat(2,minmax(0,1fr))`，避免日期/radio/joined-input 撑破列宽。
- 现有表单字段已按参考图重新分配为两列，奇数行末字段（如扩展码）独占左列。

## 数据资源目录 - 操作列不裁剪、列表下方出现横向滚动条
- 问题：`.actions` 单元格继承了 `td { overflow:hidden; text-overflow:ellipsis }`，操作按钮超过 145px 列宽时被 "..." 截掉。
- 修复：
  - 列 8 宽度 `145px → 280px`（容纳查看·查看存证·关联·变更·撤销 5 个按钮）
  - 新增 `.catalog-table td.actions { overflow:visible; text-overflow:clip; white-space:nowrap }`，即便将来按钮增多也不再被截
  - `.catalog-table` `min-width: 990px → 1280px` 与新列宽匹配
- `.table-wrap` 原有 `overflow:auto` 保留，容器宽度不足时滚动条出现在「列表内部」（而非整个页面），符合需求。
- 同步 `spec.md`：新增一条规则说明「操作列不裁剪按钮，容器宽度不足时表格下方出现横向滚动条」。

## 统一目录查询 - 新建后台模块（一级菜单）
- 新原型 `src/prototypes/unified-catalog-query/`：spec.md + index.tsx + style.css；新增 `src/pages/unified-catalog-query.html` 沿用 dev-template。
- 复用 `src/common/backend-list.css`（筛选区/表格/状态标签/分页/操作链接）和 `src/common/Layout.tsx`（公共侧边栏 + 顶部栏）；自身 style.css 只写统计卡片、表格列宽、查看弹窗、日期错误提示等本页面特有样式。
- 角色固定为「数据管理部门」。
- 操作仅【查看】，弹窗分块展示「资源名称/资源持有方/目录来源/所属领域/更新时间/目录描述」等元数据。
- 修改 `src/common/Layout.tsx`：
  - `activeMenu` 联合类型新增 `'unified-catalog-query'`；
  - **一级菜单**（用户纠正：不是备案管理二级菜单）：在「工作台」之后、「备案管理」分组之前插入 `nav-top-link`，图标用 lucide 的 `Search`（已加入 import）。
  - 面包屑重构：把原先内联的三元表达式抽成 `parentMenu` 变量；一级菜单 `parentMenu` 返回 `''`，此时面包屑中间段的 `<span>` 和分隔符都不渲染，结果为「首页 / 统一目录查询」，避免「备案管理 / 统一目录查询」的错误层级，也不会像工作台那样出现重复。
- 同步 `spec.md`：菜单层级写为一级菜单 + 面包屑无父级分类。
- 验收：`tsc --noEmit -p tsconfig.typecheck.json` 通过；`ENTRY_KEY=prototypes/unified-catalog-query npx vite build` 成功，产物 65.88 kB（gzip 18.42 kB）；`curl http://localhost:51720/prototypes/unified-catalog-query.html` 返回 200。
- 注意：`check-app-ready.mjs` 在当前环境下 spawn `npm.cmd` 报 `spawn EINVAL`，无法自行启动 dev server；改用已运行的 51720 + 单独 build 完成验收。

## 数据资源目录 - 新增弹窗字段恢复左右同行布局
- 问题：截图里「*资源名称」label 和 input 不在同一行，而是一上一下（label 全宽、input 全宽占满下一行）。
- 根因：`.modal-body label { display: block }` 选择器特异性 `(0,1,1)` 高于 `.form-field { display: flex }` `(0,1,0)`，把新增表单里的 `Field` 渲染出的 `<label class="form-field">` 也强制设成 block，input 又被 `.modal-body label input { display: block; width: 100% }` 顶到下一行全宽。
- 修复：把 `.modal-body label` 与 `.modal-body label input/select/textarea` 改成 `.modal-body label:not(.form-field)`，让 `.form-field` 的 flex 样式生效（label 和 input 回到同一行）。其它弹窗（详情/存证/确认）均无裸 `<label>`，不受影响。
- typecheck 通过。

## 数据资源目录 - 新增弹窗增加「关联政务信息资源目录」入口与子弹窗
- 「基本信息」上方加一个虚线蓝色按钮「+ 关联政务信息资源目录」（class `relate-link`）；选中项以胶囊标签 `relate-tag`（带 × 单个移除）回显在按钮右侧。
- 新组件 `RelateCatalogModal`（子弹窗）：
  - 顶部标题「关联政务信息资源目录」+ 关闭按钮
  - 查询区：资源名称输入框 + 重置 + 查询按钮（输入即过滤，点查询只重置页码）
  - 表格：复选框（支持表头全选当前页）、序号、资源名称、数据所属领域、资源摘要、操作【查看】
  - 操作列点击查看 → 三层嵌套的 `relate-detail-modal`（目录详情弹窗，z-index 1100）
  - 分页：上一页/下一页、页码（≤5 时全显示，否则显示 1–5 + … + 末页）、每页条数（10/20/50）、跳页输入框
  - 底部【取消】【确定】：确定时把已选项 ID 与对应 `GovCatalog[]` 通过 `onConfirm` 回传给 `AddResourceModal`，父表单同步更新标签
- 嵌套关闭逻辑靠 `e.stopPropagation()`：内层遮罩点不到父层 → 只关子；外层遮罩 → 关父。
- typecheck + vite build 均通过；bundle 83.87 kB（gzip 20.44 kB）。
- 同步 `spec.md`：功能清单新增「关联政务信息资源目录」条目；交互状态新增三条关于多层弹窗关闭、阻止冒泡的说明。

## 数据资源目录 - 子弹窗筛选区样式对齐列表页 filter-panel
- 把原来临时写的 `.relate-query` + `.relate-query-field`（左 label + 右 input 的 inline-flex）替换成与列表页 `.filter-panel` 同款的 `.relate-filter-panel` + `.relate-filter-actions`：
  - 外层 `display:grid; grid-template-columns:1.05fr 1fr 1.2fr 1.25fr`（4 列）；
  - `> label` 是带边框的 flex 行（label 在左、占位 `请输入` 在右）；
  - `.relate-filter-actions` `grid-column:1/-1`，承载 [查询 重置 刷新] 三个按钮（多了「刷新」按钮，对齐列表页）。
- 现在子弹窗查询区在白底卡片中、与列表页筛选用同一套视觉；typecheck + vite build 通过，bundle 83.86 kB（gzip 20.25 kB）。

## 数据资源目录 - 子弹窗筛选区 label 同行布局再次被 .modal-body label 覆盖
- 截图里「资源名称」label 和「请输入」input 又被挤成上下结构。
- 根因：`RelateCatalogModal` 自身有 `.modal-body`，里面的 `<label>` 没有 `.form-field` 类，于是 `.modal-body label:not(.form-field) { display:block }` 特异性 `(0,2,1)` 高于 `.relate-filter-panel > label { display:flex }` `(0,1,1)`，input 又被 `.modal-body label input { display:block; width:100% }` 顶到下一行。
- 修复：把 `.relate-filter-panel > label / > input / > select` 与 `.relate-filter-actions` 全部前缀 `.modal-body` 升到 `(0,3,1)`，并加 `margin-bottom:0 / width:auto / margin-top:0 / display:block` 抵消上层的 block / width:100% / margin-top:6px。
- vite build 通过，bundle 83.99 kB（gzip 20.27 kB）。
- 经验：以后在 modal-body 里的 label/input 都要写成 `.modal-body .xxx > label / > input`，或像 `.form-field` 一样用一个 modal-body 已排除的类名做避让。

## 数据资源目录 - 是/否/至今 radio/checkbox 与 label 同行，选择框缩小
- 截图里「是否涉及个人信息」的 ●○ radio 与「是」「否」文字被挤成两行；「覆盖时间范围」的「至今」也被挤到第二行，并且 radio/checkbox 被加了一圈大边框 + 全宽 padding，看起来很大。
- 根因：之前修过的 `.modal-body label:not(.form-field) { display:block; width:100% }` 同时也命中了 `<div class="radio-group"><label><input type=radio/>是</label></div>` 里的内层 label，把整组 label 推成 block、input 又占满 100% → 是/否 跑到下一行。
- 修复：
  1. 把 `.modal-body label:not(.form-field)` 限定为 `.modal-body > label:not(.form-field)`（只匹配直接子级），避免误伤内层 radio/checkbox label。
  2. 新增一组覆盖规则：`.modal-body .radio-group label` 与 `.modal-body .date-range label` 显式设 `display:inline-flex; align-items:center; gap:6px`，input/checkbox 重置 `width:auto; padding:0; border:0; width:14px; height:14px; accent-color:#1464f4`。
  3. 「选择框缩小一点」= 去掉 radio/checkbox 之前被误加的 7px/10px padding 与 1px 边框，恢复为浏览器原生 14×14 大小。
- typecheck + vite build 通过，bundle 84.62 kB（gzip 20.38 kB）。

## 数据资源目录 - 新增「变更记录」按钮（已通过且已变更行）
- 需求：第 10 条数据（id=10，状态 已通过 且 已发生过变更 = "变更已通过"）操作栏新增「变更记录」按钮，点击弹变更记录弹窗；按钮顺序调整为 撤销 · 变更记录 · 查看存证（变更记录放在撤销与查看存证之间）。
- 实现：
  - `Resource` 增加 `changed?: boolean` 与 `changeRecords?: ChangeRecord[]`（ChangeRecord={time,field,oldValue,newValue,operator}）；seed 第 10 条设 `changed:true` + 3 条示例变更记录。
  - `ActionDef.show` 签名改为 `(s: ReviewStatus, item: Resource) => boolean`；调用处 `a.show(item.reviewStatus, item)`。
  - `ALL_ACTIONS` 新增 `{ key:'changeRecord', label:'变更记录', show:(s,item)=> s==='已通过' && !!item.changed }`，位置在 撤销 之后、查看存证 之前。ActionType 联合类型同步加 'changeRecord'。
  - 新增 `ChangeRecordModal`：表格展示 变更时间/变更项/原值(红)/新值(绿)/操作人；入口接在 `action.type==='changeRecord'`。
  - CSS：`.change-record-*` 系列（head、wrap、table fixed 不溢出、old/new 值配色）。
- 操作栏顺序现为：关联 → 查看 → 编辑 → 删除 → 变更 → 撤销 → 变更记录 → 查看存证。第 10 行实际显示：关联·查看·变更·撤销·变更记录·查看存证。
- spec.md 同步更新操作按钮顺序与新增「变更记录」条目。typecheck + vite build 通过，bundle 87.06 kB（gzip 20.80 kB）。
