## 2026-09-14
新增「产品生命周期」页面，作为后台一级菜单，分配给运营机构角色。
1、页面标题：产品生命周期；模块归属：后台一级菜单（无父级，面包屑为 首页 / 产品生命周期）；角色切换器固定显示「运营机构」。
2、筛选条件：数据产品标识码、产品名称、产品类型（API产品、数据集）、产品阶段（基础产品、再开发产品）、产品提供方、当前阶段（安全审查、产品登记、产品上架、产品下架、产品交付）、阶段状态（待处理、处理中、已完成、已驳回、已终止、已暂停）、更新时间（日期范围）；操作按钮为【查询】【重置】。
3、列表字段：序号、数据产品标识码、产品名称、产品类型、产品阶段、产品提供方、当前阶段、阶段状态、更新时间、操作。
4、操作列按钮：查看、时间轴、数据血缘。
   - 查看：打开产品详情弹窗（只读，展示基本信息、阶段信息、产品简介）。
   - 时间轴：打开生命周期时间轴弹窗，按 安全审查 → 产品登记 → 产品上架 → 产品交付 → 产品下架 顺序展示各阶段流转记录（阶段、阶段状态、操作时间、操作人、处理说明），当前阶段高亮。
   - 数据血缘：打开数据血缘弹窗，分层图谱展示 数据源系统 → 数据资源 →（基础产品）→ 数据产品 →（数据服务），并附血缘关系明细表。
5、分页：共 X 条记录 + 上一页/页码/下一页 + 每页条数 + 跳页，默认 10 条/页、默认第一页。
6、页面初始化不带筛选条件加载全量数据（48 条演示数据），默认按「更新时间」倒序排列；重置后恢复全量数据；无数据时表格展示「暂无数据」。
7、后台侧边栏新增一级菜单「产品生命周期」，位于「数据资源复审」之后、「授权监管」菜单组之前。

## 2026-09-15
调整「产品生命周期」列表页「数据血缘」交互：由弹窗改为跳转独立整页。
1、列表页操作列「数据血缘」按钮改为页面跳转（`<a href>`），携带当前数据产品标识码，跳转至 `/prototypes/product-lifecycle-lineage.html?code=<数据产品标识码>`；移除原数据血缘弹窗相关的状态、渲染函数与遮罩层逻辑，列表页仅保留「查看」「时间轴」两个弹窗。
2、新增独立整页原型 `product-lifecycle-lineage`：依据 URL 参数 `code` 从演示数据定位记录并整页渲染血缘图谱、图例与血缘关系明细表，布局按整页宽度自适应；页面顶部提供「返回产品生命周期」入口，支持直接访问与刷新。
3、抽取 `lifecycle-shared.tsx`：将原列表页中的演示数据、类型定义、时间轴/血缘构建函数与血缘图谱组件统一沉淀，供列表页与数据血缘整页复用，避免重复维护。
4、同步更新 spec.md（数据血缘章节改为独立整页说明）与本文档。

## 2026-09-15（数据血缘页全屏化与节点抽屉）
重构「数据血缘」独立整页为全屏自适应血缘图谱，并新增节点抽屉交互。
1、图谱占满 `Layout` 内容区可视区域，移除页面标题栏与卡片边距；SVG 通过 `ResizeObserver` 监听容器尺寸自适应重绘。
2、血缘数据模型改为辐射式：中心为「一级产品」（蓝色），左侧为 4 个「数据资源」（橙色），右侧为 5 个「再开发产品」（绿色）；边使用带箭头的贝塞尔曲线连接。
3、节点支持点击：点击任意节点从右侧滑出抽屉面板，展示「基本信息 / 授权信息」两个页签；中心节点额外展示产品标识码、类型、行业/地域分类、提供方、简介。
4、抽屉支持点击遮罩或右上角关闭按钮收起，收起后不阻塞图谱其余交互。
5、顶部工具条左侧为「返回产品生命周期」入口，右侧为节点类型图例。
6、响应式适配：窄屏下工具条自动折行、抽屉宽度占满屏幕、抽屉信息网格改为单列布局。
7、同步更新 spec.md 与本文档。

## 2026-09-15（画布平移 / 缩放 / 重置交互）
为「数据血缘」整页图谱新增画布级交互能力，平移、缩放、重置三者兼容且叠加可用。
1、整体平移：在画布空白处按住鼠标拖拽（或单指触控拖拽），整体平移全部节点与连线；拖动超过 4px 阈值判定为平移，避免误触发节点点击。监听挂在 window 上，拖拽可超出视口且不吞掉节点 click。
2、缩放：桌面端鼠标滚轮以光标为锚点缩放，移动端双指捏合以中点为锚点缩放；缩放比例限定 40%~300% 并持续保留状态，直至主动重置或刷新。
3、重置：右下角新增悬浮控制条（放大 ＋ / 缩小 － / 重置 ⟳）并实时显示缩放百分比；点击「重置」或按 `R` 键恢复初始视图（居中、100%），`+` / `-` 键亦可步进缩放。
4、控制条与抽屉遮罩均不随画布变换，固定停在视口原位，保证操作可达且不干扰图谱其余交互。
5、同步更新 spec.md（新增「画布交互」小节）与本文档。

## 2026-09-16（数据资源节点基本信息）
「数据血缘」整页点击「数据资源」节点时，「基本信息」页签展示完整的资源详情字段。
1、新增 `DataResourceDetail` 类型与 `buildResourceDetail()` 生成器（位于 `lifecycle-shared.tsx`），为血缘左侧「数据资源」节点挂载 11 项详情：资源名称、数据资源标识码、行业分类、是否涉及个人信息、资源格式、数据来源、更新频率、覆盖时间范围、地域分类、资源持有方、资源摘要。
2、节点详情由所属数据产品的地域 / 领域推导：资源名称取节点名称，地域分类取地域可读标签，行业分类取领域，资源持有方按领域映射到对应厅局（医疗健康 → 湖南省卫生健康委信息统计中心），数据资源标识码按 `712430000MB0L046927` + 4 位地域代码 + 8 位后缀生成，其余字段取统一演示默认值（是否涉及个人信息=是、资源格式=OFD、数据来源=原始取得、更新频率=2次/天、覆盖时间范围=当年首月至至今）。
3、抽屉「基本信息」页签按节点类型分支：中心节点展示产品档案，数据资源节点展示上述 11 项资源详情，其余非中心节点维持原「所属来源 / 关联产品标识码」展示。
4、同步更新 spec.md（基本信息补充数据资源节点 11 项字段与数据说明）与本文档。

## 2026-09-16（基础产品 / 再开发产品节点基本信息）
「数据血缘」整页点击「基础产品」「再开发产品」节点（含中心节点）时，「基本信息」页签展示完整的数据产品基本信息字段。
1、新增 `DataProductDetail` 类型（16 项）与 `buildProductDetail()` 生成器（位于 `lifecycle-shared.tsx`），为血缘中全部「基础产品 / 再开发产品」节点（中心节点及非中心节点）挂载详情。
2、字段 16 项：产品名称、数据产品标识码、产品类型、覆盖时间范围、行业分类、地域分类、是否涉及个人信息、交付方式、授权使用、数据主体、数据规模、更新频率、个人或企业授权使用、产品简介、使用限制、提供方名称；顺序与用户所给样例一致。
3、字段值由所属数据产品的地域 / 领域推导：产品名称取节点名称，数据产品标识码按 `712430000MB0L046927` + 4 位地域代码 + 8 位后缀生成，产品类型取记录产品类型，行业/地域分类取领域/地域可读标签，提供方名称取记录提供方；覆盖时间范围按节点序号生成 2026-09 至 2026-10 区间，数据规模与更新频率按节点序号取演示值，其余字段取统一演示默认值。
4、抽屉「基本信息」按节点类型分支：数据资源节点展示 11 项资源详情，基础产品 / 再开发产品节点展示 16 项数据产品基本信息，原中心节点 / 非中心节点的兜底展示保留为安全分支不再常规命中。
5、同步更新 spec.md（基本信息补充 16 项字段与数据说明）与本文档。

## 2026-09-16（数据血缘抽屉页签动态化 + 授权/交易列表分页）
「数据血缘」整页抽屉页签按节点类型动态展示，并新增「授权信息」「交易信息」两个带分页的列表页签。
1、页签动态规则：数据资源节点仅显示「基本信息」；基础产品节点显示「基本信息」「授权信息」；再开发产品节点显示「基本信息」「交易信息」；中心节点依其实际类型（基础产品 / 再开发产品）归入上述规则。
2、`lifecycle-shared.tsx` 新增 `AuthRecord` / `TradeRecord` 类型与 `buildAuthList()` / `buildTradeList()` 生成器：由节点名经 FNV-1a 散列派生稳定种子，生成可复现且各节点互不相同的演示数据，列表均按时间倒序排列（授权信息按授权时间、交易信息按创建时间）。
3、授权信息列表字段：序号、运营机构、授权时间（按授权时间倒序）；交易信息列表字段：序号、数据需求方、创建时间（按创建时间倒序）；两者均配备分页组件（共 X 条记录 + 上一页/页码/下一页 + 每页条数 5/10/20），切换节点或页签时回到第一页。
4、`product-lifecycle-lineage/index.tsx` 重构抽屉渲染：新增 `DrawerList` 通用翻页列表组件与 `getTabs()` 页签规则，原「授权信息」页签静态字段（授权状态/授权对象/授权时间/授权范围）替换为授权信息列表；`lineage-style.css` 新增 `.drawer-list` / `.drawer-table` / 分页样式，复用后端列表分页类。
5、同步更新 spec.md（节点交互章节改为按类型动态页签 + 授权/交易列表说明）与本文档。

## 2026-09-16（列表筛选区「更新时间」位置调整）
「产品生命周期」列表筛选区布局调整：「更新时间」由单独第三行移至第二行第 4 格（节点状态右侧），与其他筛选项同行单列对齐。
1、`index.tsx`：将「更新时间」日期范围筛选项从独立第三行移入第二行「节点状态」之后，删除原第三行，消除多余空行。
2、`style.css`：`.filter-item-range` 由 `grid-column: span 2` 改为 `span 1`，与同行其他筛选项保持单列宽度对齐，避免字段重叠。
3、同步更新 spec.md（组合筛选改为两行排布说明）与本文档。

## 2026-09-16（时间轴弹窗节点信息补全）
「生命周期时间轴」弹窗保持时间轴展示形式不变，每个流程节点在原有阶段名称、状态标签、操作时间、处理说明基础上，新增序号与「发起信息」「审批信息」两个结构化信息块，完整覆盖流程记录字段（序号、流程节点、节点状态、单位名称、法人经办人姓名、操作时间、审核结果、审核意见）。
1、`lifecycle-shared.tsx`：`TimelineItem` 扩展 `org`（单位名称，取产品提供方）、`handler`（法人经办人姓名）、审批信息 `auditOrg`（单位名称，固定「湖南省公共数据运营中心」）、`auditHandler`（法人经办人姓名）、`auditResult`（审核结果：已完成→审核通过 / 已驳回→审核不通过 / 其他→—）、`auditOpinion`（审核意见：通过 / 「首次××不通过，材料修改后重新提交」 / —）字段，`buildTimeline` 稳定生成。
2、`index.tsx`：时间轴节点头部增加序号；节点内新增「发起信息」（单位名称、法人经办人姓名、操作时间）与「审批信息」（单位名称、法人经办人姓名、审核结果标签：绿底绿字「审核通过」/ 红底红字「审核不通过」、审核意见、操作时间）信息块；原「操作人」行由结构化信息块替代，处理说明保留。
3、`style.css`：新增 `.timeline-index`（序号徽标）、`.timeline-block / .timeline-block-title / .timeline-fields / .timeline-field`（信息块与字段行）、`.audit-tag .audit-pass / .audit-fail`（审核结果标签）样式。
4、同步更新 spec.md（时间轴节点展示内容说明）与本文档；tsc 通过（仅剩 2 处无关既有错误）。

## 2026-09-16（时间轴节点头部时间与尾部说明去除）
按截图标注调整时间轴节点内容：
1、`index.tsx`：节点头部右侧的操作时间去除（操作时间仅在「发起信息」中展示）；节点尾部「处理说明」行（如「安全审查已通过，流程流转至下一阶段」）去除。
2、同步更新 spec.md 与本文档；tsc 通过（仅剩 2 处无关既有错误）。

## 2026-09-16（审批信息增加操作时间）
按截图标注为时间轴节点「审批信息」块补充操作时间字段：
1、`lifecycle-shared.tsx`：`TimelineItem` 新增 `auditTime`（审批操作时间：已完成 / 已驳回 取节点操作时间，其余状态 → —），`buildTimeline` 生成。
2、`index.tsx`：审批信息块末尾新增「操作时间」字段（值为 `item.auditTime`）。
3、同步更新 spec.md 与本文档；tsc 通过（仅剩 2 处无关既有错误）。

## 2026-09-17（操作列新增「生命图谱」入口）
「产品生命周期」列表页操作列新增「生命图谱」入口，跳转至新建的独立整页，可视化展示产品完整的生命周期流转流程。
1、`index.tsx`：操作列在「数据血缘」之后新增「生命图谱」跳转链接（`<a href>`），携带当前数据产品标识码，跳转至 `/prototypes/product-lifecycle-graph.html?code=<数据产品标识码>`；更新顶部注释说明数据血缘、生命图谱均已改为独立整页。
2、新建独立整页原型 `product-lifecycle-graph`：以流程图（SVG）展示 开发 → 编目 → 安全审查 → 登记 → 上架 → 交易 → 下架 → 撤销 主干流转，并以橙色虚线回路体现「变更」可反复多次（上架/交易 → 变更 → 安全审查 → 登记 → 上架）；支持画布平移 / 缩放 / 重置（与数据血缘一致）与点击节点右侧抽屉展示阶段说明；顶部工具条展示当前产品标识码、名称与当前阶段，并提供「返回产品生命周期」入口；依据 URL 参数 `code` 定位记录，支持直接访问与刷新。
3、复用 `lifecycle-shared.tsx` 的 `getRecordByCode` 定位记录，不修改既有共享逻辑；样式复用 `product-lifecycle/style.css`、`product-lifecycle-lineage/lineage-style.css`、`backend-list.css`，并新增 `graph-style.css` 补充流程图与节点样式。
4、同步更新 spec.md（第 12 节新增「生命图谱独立整页」说明，功能清单与操作列、交互状态小节同步补充）与本文档。

## 2026-09-20（操作列新增「生命图谱1」入口）
「产品生命周期」列表页操作列新增与「生命图谱」并列的数据展现形式「生命图谱1」，并新建对应独立整页。
1、`index.tsx`：操作列在「生命图谱」之后，仅对当前页首行（`index === 0 && currentPage === 1`，即第一条数据）追加「生命图谱1」跳转链接（`<a href>`），携带当前数据产品标识码，跳转至 `/prototypes/product-lifecycle-graph1.html?code=<数据产品标识码>`；其余数据行仅展示「生命图谱」，原有按钮顺序与逻辑保持不变。
2、新建独立整页原型 `product-lifecycle-graph1`：以**线性时间线**形态可视化产品生命周期——严格按时间先后顺序线性排列流程节点，仅展示已实际执行过的步骤（未执行步骤一律不显示），且不存在任何支线 / 分支 / 回路，形成单一时间序列；页面通过 `?code=数据产品标识码` 定位记录，支持直接访问与刷新；点击节点从右侧抽屉展示阶段说明与操作时间；顶部工具条展示当前产品标识码、名称、当前阶段与分类图例，并提供「返回产品生命周期」入口。
3、复用 `lifecycle-shared.tsx` 的 `getRecordByCode` / `getStageStatusClass` 定位记录与状态配色；样式复用 `product-lifecycle/style.css`、`product-lifecycle-lineage/lineage-style.css`、`product-lifecycle-graph/graph-style.css`，并新增 `graph1-style.css` 补充线性时间线布局；未引入新依赖、未改动其他页面既有逻辑。
4、同步更新 spec.md（功能清单新增「生命图谱1」行、操作列说明补充首行入口）与本文档。
