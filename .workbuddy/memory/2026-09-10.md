# 2026-09-10 工作日志

- **数据资源目录变更详情：删除信息项并排对比**：在 `src/prototypes/data-resource-catalog/index.tsx` 的 `ChangeDetailInfoTable` 新增 `isFirstRecord` 入参；仅第一条变更详情中的 deleted 信息项，右侧由空占位改为展示被删除的原始内容并加红字 + 删除线（与左侧并排对齐），其余变更类型与其余变更详情保持原逻辑。`ChangeRecordModal` 通过新增 `detailIsFirst` state（`i === 0`）向 `ChangeDetailModal → ChangeDetailSide → ChangeDetailInfoTable` 透传。`style.css` 新增 `.change-detail-info-row.is-deleted-strike td { color:#d83535; text-decoration:line-through; background:#fff }`。`spec.md` 在「变更详情」条目的变更标识段补充例外规则；`change.md` 追加 2026-09-10 条目。`npm run typecheck` 无新增错误；`npm run build` 通过（29 个 HTML）；agent-browser 打开第一条变更详情并通过 DOM 计算样式验证：左侧 nianling 行 #30343b / 无删除线，右侧同内容 #d83535 / line-through，左右并排对齐。其余变更详情快照内不含 deleted 信息项（仅第一条有 `rowStatus: 'deleted'` 的 nianling），且 isFirstRecord 仅第一条为 true，逻辑不受影响。