# 2026-09-10 工作日志

- **「产品订阅监管」列表上方新增统计概览区**：在 `src/prototypes/product-subscription-supervision/index.tsx` 新增 `OVERVIEW_STATS` 模块级常量（可交易数据产品（个）/ 累计生成订单（条）/ 已交付订单（条）/ 累计调用次数（次））+ `renderOverview()`，挂载在筛选区之上（`{renderOverview()}{renderFilter()}{renderTable()}`）。样式 `.list-overview` 沿用列表卡片外壳（白底、`#dfe5ef` 边框、4px 圆角、`var(--layout-shadow)`、`margin: 0 18px 10px`），桌面一行四列 + 项间竖分隔线，`≤1024px` 两列、`≤620px` 单列。新增通用 `formatThousands()` 千分位函数，金额列 `formatAmountInCents()` 改为委托它（行为不变）。**统计口径为全量，不随列表筛选联动**——刻意不参与 `filteredList`/`useMemo` 计算。`spec.md` 新增 §3.1，`change.md` 追加条目。`tsc --noEmit` 无新增错误，dev server 编译通过。
- **「订阅明细」新增「操作」列 + 调用明细弹窗（仅 API 产品）**：订阅明细表格在「更新时间」后新增「操作」列，`isApiProduct = currentRecord?.productType === 'API产品'` 为真时才渲染【调用明细】按钮（非 API 列保留、单元格为空）。点击打开调用明细弹窗，实现**完全复用 `subscription-order-supervision`（订单交付监管）页的调用明细**：延时提示条 + 手动刷新、四项汇总统计（千分位）、调用状态/调用时间筛选 + 查询/重置、调用日志表格（状态失败红色高亮、响应时长、调用时间）+ 分页、加载中/失败兜底/暂无记录三态、失败行「查看失败原因」子弹窗。新增 `CallLog` 类型、`callLogsByOrder`/`callStatsByOrder`（按 orderNo 索引，**仅为 API 产品订单生成**）、`CALL_STATUS_OPTIONS`/`TIME_RANGE_OPTIONS`/`TIME_RANGE_CUTOFF`/`CALL_ERRORS`；新增 `currentOrder` 状态承载被点击订单。样式 `.call-tip`/`.link-btn`/`.call-stats-grid`/`.call-filter-section`（`filter-section.call-filter-section` 需提高特异性覆盖后加载的 backend-list.css 契约）`.call-status-fail`/`.call-log-table` 沿用订单交付监管页取值。明细表格 6→7 列，`colSpan` 同步改 7。
