
## 2026-09-16 产品生命周期筛选区布局
- 列表筛选区「更新时间」由独立第三行移至第二行第 4 格（节点状态右侧），单列日期范围；.filter-item-range 由 span 2 改为 span 1；同步更新 spec.md / change.md；tsc 通过（仅剩 2 处无关既有错误）。

## 2026-09-16 时间轴弹窗节点信息补全
- 时间轴弹窗保持展示形式不变，每节点新增序号+发起信息（单位名称/法人经办人姓名/操作时间）+审批信息（审核结果标签/审核意见）两个结构化块；TimelineItem 扩展 org/handler/auditResult/auditOpinion；style.css 新增 timeline-block、audit-tag 样式；tsc 通过（仅 2 处无关既有错误）。

## 2026-09-16 时间轴节点头部时间与尾部说明去除
- 节点头部右侧操作时间去除（仅保留在发起信息内）；节点尾部处理说明行去除；同步更新 spec.md/change.md；tsc 通过（仅 2 处无关既有错误）。
