# 2026-08-31
- 数据资源目录模块：将 AddResourceModal 重构为共用 ResourceForm（标题/初始数据差异化）。新增 EditResourceModal、ChangeResourceModal，字段与新增完全一致（基本信息两步 + 资源持有方信息 + 信息项），编辑/变更弹窗通过 buildInitial(item) 回显资源名称、行业分类（取首个细分项，若不在粗分类下拉则自动补为可选项）、资源持有方（提供机构）。操作列 edit/change 不再走 ConfirmModal，改为直接渲染对应表单弹窗（独立 overlay）。已通过 tsc 类型检查与 SSR 渲染验证。
- 数据资源目录模块：已选中的关联政务信息资源目录胶囊标签尾部删除按钮升级为红色圆形 ×（.relate-tag-remove，#f04444 实心圆 / 内嵌白色 strokeWidth=3 的 Lucide X）。hover 加深到 #d83535 + 1.08x 放大 + 红色光晕；active #c42929 + 0.96x 按下反馈。无障碍：按钮加 aria-label（如「移除「气温变化」」）和 title="移除"。删除逻辑保持不变：setRelatedItems(prev => prev.filter(x => x.id !== item.id))。spec.md 同步：在「关联政务信息资源目录」功能说明里追加「每个标签尾部附带红色圆形 × 按钮，点击即可移除该条已选目录」。tsc 类型检查通过。
- 数据资源目录模块：资源目录关联弹窗（RelateCatalogModal）按规格重写——标题改为「资源目录关联」；头部右侧追加「已关联政务信息资源目录：{name}」蓝色可点击链接（点击唤起详情弹窗）+ 关闭 ×，链接由父表单通过 initialAssociated={relatedItems[0] ?? null} 注入。表格改为单选模式（prev.includes(id) ? [] : [id]），已勾选行加 .selected-row 高亮（#e7f1ff）；筛选区按钮顺序调整为【重置】【查询】；【确定】按钮做未勾选校验，提示「请选择需要关联的资源目录」并以 .confirm-error 红色右对齐展示，勾选任何行即清除提示。查询/重置/翻页均保留已勾选 id（state 不变）。tsc 类型检查通过；SSR 渲染验证新结构正常：标题、已关联信息、筛选按钮、表头、单选高亮、确认校验均符合预期。spec.md 同步在「关联政务信息资源目录」条目补充新结构说明。
- 修复：数据资源目录列表操作列「关联」按钮此前只弹了一个无用的 ConfirmModal（标题“关联数据源”），真正的资源目录关联弹窗 RelateCatalogModal 从未挂载，导致用户点「关联」看不到关联弹窗。改为：操作列 action.type==="relate" 时直接挂载 <RelateCatalogModal initialSelected={[]} initialAssociated={null} onClose onConfirm>；并把 relate 从通用 ConfirmModal 包裹层(delete/revoke/changeRecord)中剔除，避免双层遮罩。tsc 类型检查通过。
- 数据资源目录模块：关联弹窗头部「已关联政务信息资源目录」区域升级为芯片列表——初始关联项渲染为蓝色椭圆芯片（每项可点击唤起详情）；用户勾选表格行时，若该条目已在初始列表内，对应芯片在头部同步高亮（白字 #1464f4 实心 + ✓ 前缀）；若选中的不在初始列表内，会在头部额外追加一颗带「当前选中」标签的虚线描边芯片，保证顶部与表格行底色两处选中状态完全一致；空状态（既无初始关联也无选中）下整个区域隐藏。改动：initialAssociated prop 由 GovCatalog \| null 改为 GovCatalog[]；新增 lucide-react 的 Check 图标；CSS 新增 .head-associated-list / .head-associated-chip / .current 高亮 / .new-selection 虚线 / .chip-tag 内联小标签；ResourceForm 调用处传 relatedItems 全数组，操作列「关联」调用处传 []。tsc 通过；SSR 四种状态（A=2 初始、 B=0+1 选中、C=1+同选中、D=全空）渲染均符合预期；spec.md 同步。
- 数据资源目录模块：关联弹窗头部「已关联政务信息资源目录」芯片区按用户要求从弹窗头部下移到正文区顶部、置于「资源名称」查询条件之上。头部现在只剩标题 + 关闭 ×；正文区顶部新增 .relate-associated-panel 容器（淡蓝底 #f5f8ff + 细边框 #e0eaf8 + 圆角 + 10/14 padding + 12px 下间距），内部布局沿用 head-associated-label + head-associated-list + chip 体系，选中态同步逻辑保持不变（初始关联项 chips、当前选中高亮 ✓、未在初始列表则额外追加「当前选中」虚线芯片）。tsc 类型检查通过；SSR 三种状态（A=2 初始、B=0+1 选中、C=0+0）渲染正确：panel 始终位于 filter 之前，head-aside 不再含芯片，空状态下整个 panel 隐藏。spec.md 同步。
- 数据资源目录模块：新增/编辑/变更三弹窗共用 ResourceForm，新增 mode 参数（add|edit|change）以区分行为。变更弹窗两项差异：1) 第二步「信息项」表格下方新增「变更说明」必填 textarea（Field full 整行，rows=3，状态 changeNote，FormInitial 同步加 changeNote?: string），仅在 mode==="change" 时渲染；2) 底部按钮去掉「存为草稿」（改为 mode !== change 才渲染），变更为 取消 / 上一步 / 提交。CSS 新增 .add-body>.form-field.full（margin-top 14px、label 宽 110px 右对齐、textarea 可垂直拉伸）。tsc 通过；SSR 验证变更弹窗有变更说明且无存为草稿、新增弹窗有存为草稿且无变更说明。spec.md 同步。
